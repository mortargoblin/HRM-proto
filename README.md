# H(A)RAM System

Heart Rate And More System


## Instructions

1. Install dependencies
`pip install mpremote`

2. Clone the repository
`git clone `

3. Run the install script
with Bash:
`./install.sh`

OR

with powershell or windows command prompt:
`.\install.cmd`
